# ERC-4337 Research Plan

## Purpose

Track research into interactions among ERC-4337, MIP-4, and EIP-7702 delegated EOAs.

## Current Verified Facts

- A protocol-created EIP-7702 delegated EOA in a real Monad Testnet authorization-list transaction produced `dippedIntoReserve() == true` while its balance moved from 19 MON to 9 MON.
- The successful transaction was sponsor-submitted.
- In the sponsor-submitted Testnet path, exactly 10 MON did not dip, while 10 MON minus 1 wei did dip.
- In a current-balance Testnet comparison, both sponsor-submitted and delegated-authority-submitted type-4 transactions dipped when the delegated authority was decremented below 10 MON.
- In a below-reserve Testnet path, a delegated EOA starting at 9 MON and remaining unchanged succeeded with `false -> false -> false`; attempting to decrement to 8 MON reverted.
- `examples/mip4-sca` now contains a MIP-4-aware ERC-4337 smart account example built around a guarded EIP-7702 delegated account.
- The `mip4-sca` example includes Forge unit tests, an `anvil --monad` integration suite, and Monad testnet demo scripts.

## System Map

| Component | Research role |
| --- | --- |
| `UserOperation` | Encodes the account action that may change the delegated EOA balance |
| `EntryPoint` | Executes and sequences operations in a bundle |
| Bundler | Submits the bundle transaction |
| Paymaster | May sponsor gas and alter balance-flow assumptions |
| Delegated EOA | Candidate account whose reserve state is observed |
| Reserve-balance violation | State transition under investigation |
| `dippedIntoReserve()` | Observation mechanism to sample at defined checkpoints |

Core question:

```text
Can an ERC-4337-style operation involving a delegated EOA cause dippedIntoReserve() == true,
and can that violation be detected or isolated inside a bundle?
```

## Open Questions

- Which address is classified as the transaction sender, sponsor, caller, and affected account at each layer?
- Can one operation's reserve state be observed by another operation in the same bundle?
- Does a paymaster-funded flow change the relevant balance transition?
- Where should observations occur to distinguish operation-local and transaction-wide state?
- Which `mip4-sca` observations are verified on Testnet, which are verified with `anvil --monad`, and which are only unit-test coverage?
- Should the guard live in the account implementation, an ERC-7579 hook, bundler simulation, paymaster policy, wallet UX, or some combination?

## Next Steps

- Review `examples/mip4-sca` against this plan and record the verified behavior separately from demo assumptions.
- Update Issue #5 and Issue #12 so they reflect the current implementation instead of treating ERC-4337 as future work.
- Decide whether paymaster-funded flows need a separate experiment.
- Decide whether bundler simulation should become its own implementation or documentation task.
