[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.28"
evm_version = "prague"
network = "monad"
optimizer = true
optimizer_runs = 1000
# MIP-4 reserve tracking is transaction-scoped.
isolate = true

[rpc_endpoints]
monad_testnet = "${MONAD_TESTNET_RPC}"

[fmt]
line_length = 120
